=begin
#Pulp 3 API

#Fetch, Upload, Organize, and Distribute Software Packages

The version of the OpenAPI document: v3
Contact: pulp-list@redhat.com
Generated by: https://openapi-generator.tech
Generator version: 7.20.0

=end

require 'date'
require 'time'

module PulpOstreeClient
  # A Serializer class for uncategorized content units (e.g., static deltas).
  class OstreeOstreeContent < ApiModelBase
    # A URI of a repository the new content unit should be associated with.
    attr_accessor :repository

    # A dictionary of arbitrary key/value pairs used to describe a specific Content instance.
    attr_accessor :pulp_labels

    # Artifact file representing the physical content
    attr_accessor :artifact

    attr_accessor :relative_path

    attr_accessor :digest

    # Attribute mapping from ruby-style variable name to JSON key.
    def self.attribute_map
      {
        :'repository' => :'repository',
        :'pulp_labels' => :'pulp_labels',
        :'artifact' => :'artifact',
        :'relative_path' => :'relative_path',
        :'digest' => :'digest'
      }
    end

    # Returns attribute mapping this model knows about
    def self.acceptable_attribute_map
      attribute_map
    end

    # Returns all the JSON keys this model knows about
    def self.acceptable_attributes
      acceptable_attribute_map.values
    end

    # Attribute type mapping.
    def self.openapi_types
      {
        :'repository' => :'String',
        :'pulp_labels' => :'Hash<String, String>',
        :'artifact' => :'String',
        :'relative_path' => :'String',
        :'digest' => :'String'
      }
    end

    # List of attributes with nullable: true
    def self.openapi_nullable
      Set.new([
      ])
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(attributes = {})
      if (!attributes.is_a?(Hash))
        fail ArgumentError, "The input argument (attributes) must be a hash in `PulpOstreeClient::OstreeOstreeContent` initialize method"
      end

      # check to see if the attribute exists and convert string to symbol for hash key
      acceptable_attribute_map = self.class.acceptable_attribute_map
      attributes = attributes.each_with_object({}) { |(k, v), h|
        if (!acceptable_attribute_map.key?(k.to_sym))
          fail ArgumentError, "`#{k}` is not a valid attribute in `PulpOstreeClient::OstreeOstreeContent`. Please check the name to make sure it's valid. List of attributes: " + acceptable_attribute_map.keys.inspect
        end
        h[k.to_sym] = v
      }

      if attributes.key?(:'repository')
        self.repository = attributes[:'repository']
      end

      if attributes.key?(:'pulp_labels')
        if (value = attributes[:'pulp_labels']).is_a?(Hash)
          self.pulp_labels = value
        end
      end

      if attributes.key?(:'artifact')
        self.artifact = attributes[:'artifact']
      else
        self.artifact = nil
      end

      if attributes.key?(:'relative_path')
        self.relative_path = attributes[:'relative_path']
      else
        self.relative_path = nil
      end

      if attributes.key?(:'digest')
        self.digest = attributes[:'digest']
      else
        self.digest = nil
      end
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      warn '[DEPRECATED] the `list_invalid_properties` method is obsolete'
      invalid_properties = Array.new
      if @artifact.nil?
        invalid_properties.push('invalid value for "artifact", artifact cannot be nil.')
      end

      if @relative_path.nil?
        invalid_properties.push('invalid value for "relative_path", relative_path cannot be nil.')
      end

      if @relative_path.to_s.length < 1
        invalid_properties.push('invalid value for "relative_path", the character length must be greater than or equal to 1.')
      end

      if @digest.nil?
        invalid_properties.push('invalid value for "digest", digest cannot be nil.')
      end

      if @digest.to_s.length < 1
        invalid_properties.push('invalid value for "digest", the character length must be greater than or equal to 1.')
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      warn '[DEPRECATED] the `valid?` method is obsolete'
      return false if @artifact.nil?
      return false if @relative_path.nil?
      return false if @relative_path.to_s.length < 1
      return false if @digest.nil?
      return false if @digest.to_s.length < 1
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] artifact Value to be assigned
    def artifact=(artifact)
      if artifact.nil?
        fail ArgumentError, 'artifact cannot be nil'
      end

      @artifact = artifact
    end

    # Custom attribute writer method with validation
    # @param [Object] relative_path Value to be assigned
    def relative_path=(relative_path)
      if relative_path.nil?
        fail ArgumentError, 'relative_path cannot be nil'
      end

      if relative_path.to_s.length < 1
        fail ArgumentError, 'invalid value for "relative_path", the character length must be greater than or equal to 1.'
      end

      @relative_path = relative_path
    end

    # Custom attribute writer method with validation
    # @param [Object] digest Value to be assigned
    def digest=(digest)
      if digest.nil?
        fail ArgumentError, 'digest cannot be nil'
      end

      if digest.to_s.length < 1
        fail ArgumentError, 'invalid value for "digest", the character length must be greater than or equal to 1.'
      end

      @digest = digest
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          repository == o.repository &&
          pulp_labels == o.pulp_labels &&
          artifact == o.artifact &&
          relative_path == o.relative_path &&
          digest == o.digest
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [repository, pulp_labels, artifact, relative_path, digest].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      attributes = attributes.transform_keys(&:to_sym)
      transformed_hash = {}
      openapi_types.each_pair do |key, type|
        if attributes.key?(attribute_map[key]) && attributes[attribute_map[key]].nil?
          transformed_hash["#{key}"] = nil
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[attribute_map[key]].is_a?(Array)
            transformed_hash["#{key}"] = attributes[attribute_map[key]].map { |v| _deserialize($1, v) }
          end
        elsif !attributes[attribute_map[key]].nil?
          transformed_hash["#{key}"] = _deserialize(type, attributes[attribute_map[key]])
        end
      end
      new(transformed_hash)
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {}
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.include?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

  end

end
